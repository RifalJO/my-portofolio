export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-xs uppercase tracking-[0.3em] text-[#006E1C] font-[family-name:var(--font-body)] mb-4">
      {children}
    </p>
  )
}
